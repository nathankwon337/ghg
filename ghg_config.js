/*!
 * ghg_config.js — 온실가스 관리기사 학습 플랫폼 클라우드 동기화 설정 (선택 사항)
 * ------------------------------------------------------------------
 * gamify_config.js와 동일한 패턴입니다. 이 파일을 비워두면(또는 아예 지우면)
 * 클라우드 동기화 없이 이 브라우저 안에서만 오답노트·마킹·학습 결과가 저장됩니다.
 *
 * 기기 간 동기화를 켜고 싶다면:
 *   1) 이 프로젝트의 apps_script_Code_gs.txt를 Google 스프레드시트의
 *      "확장 프로그램 > Apps Script"에 붙여넣고 웹앱으로 배포합니다.
 *      (다른 게임들이 이미 쓰고 있는 배포를 그대로 재사용해도 됩니다 —
 *       이 앱은 "ghg::닉네임" 형태로 별도 행에 저장되므로 겹치지 않습니다.)
 *   2) 배포 후 나온 '.../exec' 웹앱 URL을 아래 따옴표 안에 붙여넣습니다.
 *   3) 앱 우측 상단의 "☁ 동기화" 버튼에서 닉네임을 설정하면 그때부터
 *      오답노트/마킹/학습 결과가 자동으로 클라우드에 백업되고,
 *      다른 기기에서 같은 닉네임으로 "☁ 불러오기"를 누르면 복원됩니다.
 *
 * 참고: 문제 세트(엑셀로 업로드한 100문항 데이터) 자체는 동기화 대상이
 * 아닙니다. 각 기기에서 엑셀 파일을 한 번씩 업로드해주면 됩니다.
 */
window.GHG_CLOUD_ENDPOINT = '';
