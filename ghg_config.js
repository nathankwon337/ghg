/*!
 * ghg_config.js — 온실가스 관리기사 학습 플랫폼, 오답노트 클라우드 동기화 "웹앱 주소" 기본값 (선택 사항)
 * ------------------------------------------------------------------
 * gamify_config.js와 동일한 패턴입니다. 이 파일이 없거나 아래 값이 비어 있으면,
 * 클라우드 동기화 없이 이 브라우저 안에서만 오답노트(전체 오답 · 빈번오답 · 마킹)가 저장되며
 * 앱은 평소와 똑같이 동작합니다.
 *
 * 설정 방법:
 *   1) 이 앱의 "오답노트 → ⚙ 오답노트 동기화 설정" 모달 안에 있는
 *      "웹앱 주소는 어떻게 만드나요?"를 펼치면 Google Apps Script 코드가 들어있습니다.
 *      Google 스프레드시트 하나를 새로 만들고 "확장 프로그램 > Apps Script"에 그 코드를
 *      붙여넣은 뒤 웹앱으로 배포하세요.
 *      (주의: 다른 학습 게임들의 gamify 클라우드 동기화와는 데이터 형식이 다르므로,
 *       반드시 새 스프레드시트를 따로 만들어서 쓰세요. 같은 시트를 공유하면 안 됩니다.)
 *   2) 배포 후 나온 '.../exec' 웹앱 URL을 아래 따옴표 안에 붙여넣습니다.
 *   3) 이 파일을 저장하고 온실가스관리기사_학습플랫폼.html과 같은 폴더/저장소에 두면,
 *      그때부터는 누구든 "동기화 코드"만 입력하면 자동으로 이 서버를 통해 여러 기기의
 *      오답노트가 합쳐집니다 (웹앱 주소를 각자 입력할 필요가 없어짐).
 *
 * 참고: 문제 세트(엑셀로 업로드한 100문항 데이터) 자체는 동기화 대상이 아닙니다.
 * 각 기기에서 엑셀 업로드 또는 "🔄 동기화"의 깃허브 불러오기로 한 번씩 등록하면 됩니다.
 */
window.GHG_CLOUD_ENDPOINT = 'https://script.google.com/macros/s/AKfycbxDlkxxNR58U-WmhPJhOvf2Et8KNR8Ksg6-ijpciaOY2lwSagrfq3V_XkETNStKD_PX/exec';
